% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hpc_pipeline.R
\name{estimate}
\alias{estimate}
\alias{estimate.default}
\alias{estimate.tidytargets}
\title{Estimate genes on a tidytargets pipeline}
\usage{
estimate(
  input_hpc,
  formula_abundance,
  formula_dispersion = ~1,
  offset,
  dispersion = NULL,
  dispersion_degrees_freedom = NULL,
  ...
)

\method{estimate}{default}(
  input_hpc,
  formula_abundance,
  formula_dispersion = ~1,
  offset,
  dispersion = NULL,
  dispersion_degrees_freedom = NULL,
  ...
)

\method{estimate}{tidytargets}(
  input_hpc,
  formula_abundance,
  formula_dispersion = ~1,
  offset,
  dispersion = NULL,
  dispersion_degrees_freedom = NULL,
  bundle = 10L,
  target_output = "brms_fit",
  ...
)
}
\arguments{
\item{input_hpc}{A \code{tidytargets} / \code{brmDE_hpc} pipeline from \code{\link[=brmDE]{brmDE()}}.}

\item{formula_abundance}{Model for the mean, passed to \code{\link[=estimate_gene]{estimate_gene()}}.}

\item{formula_dispersion}{One-sided model for the negative binomial shape,
passed to \code{\link[=estimate_gene]{estimate_gene()}}. \code{~1} by default.}

\item{offset}{Required name of the precomputed offset column in \code{colData}
(the same argument as \code{\link[=estimate_gene]{estimate_gene()}}).}

\item{dispersion}{Optional name of the \code{rowData} dispersion column, as
written by \code{\link[tidybulk:estimate_dispersion]{tidybulk::estimate_dispersion()}}. Default \code{NULL} puts a zero
offset on the shape submodel (see \code{\link[=estimate_gene]{estimate_gene()}}).}

\item{dispersion_degrees_freedom}{Optional name of the effective degrees of
freedom column written alongside it by \code{\link[tidybulk:estimate_dispersion]{tidybulk::estimate_dispersion()}}.
Default \code{NULL} uses a default Student-t SD of 1 on the log-shape intercept.}

\item{...}{Passed to \code{\link[=estimate_gene]{estimate_gene()}} (e.g. \code{family}, \code{chains},
\code{draws_sampling}).}

\item{bundle}{Number of genes to fit per target. Default \code{10}. \code{1} gives
one target per gene. A larger value fits that many genes sequentially
inside one target, which is how you stop tens of thousands of genes from
swamping an HPC scheduler with tiny jobs. See \emph{Bundling} below.}

\item{target_output}{Name of the targets output.}
}
\value{
The updated \code{tidytargets} pipeline.
}
\description{
Appends a \code{\link[tidytargets:tt_iterate]{tidytargets::tt_iterate()}} step that calls \code{\link[=estimate_gene]{estimate_gene()}} once
per gene.
}
\details{
Neither the offset nor the dispersion is computed here. Run
\code{\link[tidybulk:estimate_dispersion]{tidybulk::estimate_dispersion()}} on the whole object before \code{\link[=brmDE]{brmDE()}},
exactly as you compute the offset, so that both are ordinary columns of the
input, then pass those column names here. Gene-wise fitting is the only
thing this pipeline does. The dispersion columns are a prior on the
negative binomial shape (see \code{\link[=estimate_gene]{estimate_gene()}}): they inform each gene
without fixing the posterior to the external estimate. Omitting
\code{dispersion} and/or \code{dispersion_degrees_freedom} is valid (zero shape
offset, default prior SD) but computing and passing both is the preferred
starting point.

Prior constants derived from each gene are passed to Stan as data, so every
gene generates identical Stan code and cmdstanr compiles it at most once per
worker process rather than once per gene.
}
\section{Where the fits are}{

A \code{brmsfit} carries its draws and its data, so at 20,000 genes the fits are
far too large to be gathered into one table, and gathering them would undo
the point of having a store. \code{\link[=estimate]{estimate()}} therefore contributes the gene
column alone; what makes the result worth reading is \code{\link[=hypothesis]{hypothesis()}}, whose
per-contrast statistics and convergence are small enough to hold for every
gene at once (see \code{\link[=hypothesis_gene]{hypothesis_gene()}}). The fits stay in the store. Read
one with \code{targets::tar_read(brms_fit, branches = i, store = store)}, using
the same \code{store} you passed to \code{\link[=brmDE]{brmDE()}}; with \code{bundle = 1} the branch
index is the gene's row in the result.
}

\section{What the pipeline remembers}{

What the fits were asked for goes onto the pipeline with
\code{\link[tidytargets:tt_metadata]{tidytargets::tt_metadata()}}: the formulas, the column names, and every
\code{\link[=estimate_gene]{estimate_gene()}} argument the sampling depends on, defaults included, so
the object carries its own specification while the fits stay in the store.
Read it with \code{tt_metadata(pipeline)}. It is how \code{\link[=plot_volcano]{plot_volcano()}} knows the
\code{chains * draws_sampling} draws a \code{pH0} was counted from, and so why it
takes the pipeline rather than the table.
}

\section{Bundling}{

One target per gene gives the scheduler tens of thousands of jobs whose
queueing cost rivals the fit itself. \code{bundle} puts that many genes in each
fit target instead, and \code{\link[=hypothesis]{hypothesis()}} and \code{\link[=adjust]{adjust()}} inherit the coarser
branching because they map over the fit target. The result of
\code{\link[=tt_evaluate]{tt_evaluate()}} is unchanged: one row per gene, in the same order.

Size \code{bundle} against one job rather than against the gene count, since
that is what it controls.
\itemize{
\item A bundle is fitted sequentially in one worker, so it needs about \code{bundle}
times the walltime of a single fit.
\item A bundle is one \code{.rds} holding \code{bundle} fits, and it is read back into
the main process, so the size of a \code{brmsfit} is what limits how far you
can push this.
\item Invalidation is per bundle. One gene failing loses its bundle, and
changing the gene set reshuffles bundle membership and refits everything.
Leave \code{bundle} at \code{1} when you rely on incremental reruns.
}
}

