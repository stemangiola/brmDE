% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hpc_pipeline.R
\name{brmDE}
\alias{brmDE}
\alias{print.brmDE_hpc}
\title{Start a gene-wise tidytargets pipeline}
\usage{
brmDE(
  .data,
  abundance = "counts",
  features = NULL,
  store = tempfile(tmpdir = tempdir(), pattern = "brmde_"),
  computing_resources = NULL,
  debug_step = NULL,
  callr_function = NULL,
  packages = c("tidytargets", "brmDE", "SummarizedExperiment", "brms", "tibble"),
  update = "thorough",
  garbage_collection = TRUE,
  workspace_on_error = FALSE,
  verbosity = targets::tar_config_get("reporter_make")
)

\method{print}{brmDE_hpc}(x, ...)
}
\arguments{
\item{.data}{A \code{SummarizedExperiment} that already carries a library size
offset in \code{colData} and dispersion in \code{rowData}. Neither is
computed here: both are whole-matrix quantities, so derive them before
the pipeline (\code{tidybulk::scale_abundance()} taking \code{log(1 / multiplier)},
and \code{\link[tidybulk:estimate_dispersion]{tidybulk::estimate_dispersion()}}) and name the columns when you call
\code{\link[=estimate]{estimate()}}.}

\item{abundance}{Count assay name.}

\item{features}{Optional character vector of gene ids to fit, e.g.
\code{c("gene1", "gene2")}. Subsetting here only limits which genes are
fitted; the offset and dispersion already reflect all genes.}

\item{store}{Directory used as the targets store. The script is
\verb{\{store\}.R}.}

\item{computing_resources}{A crew controller, or \code{NULL} for sequential
targets (default). On SLURM, use
\code{crew.cluster::crew_controller_slurm()}; see the vignette section
\emph{SLURM} for an example.}

\item{debug_step}{Optional target name for \code{tar_option_set(debug = )}.}

\item{callr_function}{Passed to \code{\link[targets:tar_make]{targets::tar_make()}} by
\code{\link[=tt_evaluate]{tt_evaluate()}}. \code{NULL} (default) runs in the current session.}

\item{packages}{Character vector of packages loaded in workers.}

\item{update, garbage_collection, workspace_on_error}{Passed through
to \code{tar_option_set()} / \code{tar_cue()} as in tidytargets.}

\item{verbosity}{Reporter passed to \code{\link[targets:tar_make]{targets::tar_make()}}.}
}
\value{
A \code{tidytargets} pipeline object (\code{brmDE_hpc}). The graph is not
run until you print it or call \code{\link[=tt_evaluate]{tt_evaluate()}}.
}
\description{
Analogue of tidytargets' \code{\link[tidytargets:tt_initialise]{tidytargets::tt_initialise()}}: writes the targets
header (\code{target_list}) and the shared steps (load SE, gene ids). Later
calls to \code{\link[=estimate]{estimate()}}, \code{\link[=hypothesis]{hypothesis()}}, and \code{\link[=adjust]{adjust()}} append
\code{tt_iterate()} branches onto the same graph. Printing the object (or
calling \code{\link[=tt_evaluate]{tt_evaluate()}}) runs the pipeline, as in tidytargets. Assigning
it does not; an interactive session then says the pipeline is ready to be
evaluated, rather than appearing to do nothing.
}
\details{
The pipeline is gene-wise only. Anything estimated across the whole matrix,
namely the library size offset and the dispersion, belongs upstream of
\code{\link[=brmDE]{brmDE()}} and arrives as ordinary \code{colData} / \code{rowData} columns.

Running the pipeline gives one row per gene and contrast. \code{\link[=estimate]{estimate()}}
contributes the gene column; \code{\link[=hypothesis]{hypothesis()}} adds its columns inline, each
carrying the convergence of that contrast, and \code{\link[=adjust]{adjust()}} its own table.
The fits are not in that
result, because a transcriptome's worth of \code{brmsfit} objects will not fit
in one table: they stay in the store. Read one with
\code{targets::tar_read(brms_fit, branches = i, store = store)}, using the same
\code{store} you passed to \code{\link[=brmDE]{brmDE()}}. See \emph{Where the fits are} in \code{\link[=estimate]{estimate()}}.
}
\examples{
\dontrun{
data("airway", package = "airway")

# Whole-matrix quantities are prepared before the gene-wise pipeline.
se <- airway
se$offset <- log(colSums(SummarizedExperiment::assay(se, "counts")))
se <- tidybulk::estimate_dispersion(se, formula_abundance = ~ dex + cell)

se |>
  brmDE(features = c("ENSG00000120129")) |>
  estimate(
    ~ dex + (1 | cell),
    offset = "offset",
    dispersion = "dispersion",
    dispersion_degrees_freedom = "dispersion_degrees_freedom",
    family = brms::negbinomial()
  ) |>
  hypothesis("dextrt = 0") |>
  adjust(nullify = "dex")

# SLURM (from a cluster login node; requires crew.cluster):
# library(crew.cluster)
# se |>
#   brmDE(
#     store = "/path/to/brmde_store",
#     computing_resources = crew_controller_slurm(
#       workers = 100,
#       options_cluster = crew_options_slurm(
#         memory_gigabytes_per_cpu = 5
#       )
#     )
#   ) |>
#   estimate(~ dex + (1 | cell), offset = "offset", dispersion = "dispersion",
#            dispersion_degrees_freedom = "dispersion_degrees_freedom")
}

}
