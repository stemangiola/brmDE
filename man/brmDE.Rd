% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hpc_pipeline.R
\name{brmDE}
\alias{brmDE}
\alias{print.brmDE_hpc}
\title{Start a gene-wise HPCell pipeline}
\usage{
brmDE(
  .data,
  abundance = "counts",
  method = "TMM",
  features = NULL,
  store = tempfile(tmpdir = tempdir(), pattern = "brmde_"),
  computing_resources = NULL,
  debug_step = NULL,
  callr_function = NULL,
  packages = c("HPCell", "brmDE", "tidybulk", "SummarizedExperiment", "brms", "tibble",
    "edgeR"),
  update = "thorough",
  garbage_collection = TRUE,
  workspace_on_error = FALSE,
  verbosity = targets::tar_config_get("reporter_make")
)

\method{print}{brmDE_hpc}(x, ...)
}
\arguments{
\item{.data}{A \code{SummarizedExperiment}. Offset is calculated on all genes
in this object.}

\item{abundance}{Count assay name.}

\item{method}{tidybulk scaling method for the offset (\code{"TMM"} or
\code{"TMMwsp"}).}

\item{features}{Optional character vector of gene ids to fit, e.g.
\code{c("gene1", "gene2")}. TMM is still computed on all genes in \code{.data}.}

\item{store}{Directory used as the targets store. The script is
\verb{\{store\}.R}.}

\item{computing_resources}{A crew controller, or \code{NULL} for sequential
targets (default).}

\item{debug_step}{Optional target name for \code{tar_option_set(debug = )}.}

\item{callr_function}{Passed to \code{\link[targets:tar_make]{targets::tar_make()}} by
\code{\link[=evaluate_hpc]{evaluate_hpc()}}. \code{NULL} (default) runs in the current session.}

\item{packages}{Character vector of packages loaded in workers.}

\item{update, garbage_collection, workspace_on_error}{Passed through
to \code{tar_option_set()} / \code{tar_cue()} as in HPCell.}

\item{verbosity}{Reporter passed to \code{\link[targets:tar_make]{targets::tar_make()}}.}
}
\value{
An \code{HPCell} pipeline object (\code{brmDE_hpc}).
}
\description{
Analogue of HPCell's \code{\link[HPCell:initialise_hpc]{HPCell::initialise_hpc()}}: writes the targets
header (\code{target_list}) and the shared steps (load SE, tidybulk offset,
gene ids). Later calls to \code{\link[=estimate]{estimate()}}, \code{\link[=hypothesis]{hypothesis()}}, and \code{\link[=adjust]{adjust()}}
append \code{hpc_iterate()} branches onto the same graph. Printing the object
(or calling \code{\link[=evaluate_hpc]{evaluate_hpc()}}) runs the pipeline, as in HPCell.
}
\examples{
\dontrun{
data("airway", package = "airway")
airway |>
  brmDE(features = c("ENSG00000120129")) |>
  estimate(
    ~ dex + (1 | cell),
    offset = "offset",
    dispersion = "dispersion",
    family = brms::negbinomial()
  ) |>
  hypothesis("dextrt = 0") |>
  adjust(nullify = "dex")
}

}
