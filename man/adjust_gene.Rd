% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_gene.R
\name{adjust_gene}
\alias{adjust_gene}
\title{Adjust one gene by removing unwanted effects}
\usage{
adjust_gene(
  fit,
  nullify = NULL,
  newdata = NULL,
  re_formula = ~0,
  offset = "offset",
  offset_value = 0,
  robust = TRUE,
  correct_by_offset = TRUE,
  sample_id = NULL
)
}
\arguments{
\item{fit}{A \code{brmsfit} from \code{\link[=estimate_gene]{estimate_gene()}}.}

\item{nullify}{Character vector of covariate names to set to \code{NA} in
\code{newdata} (nuisance terms). Ignored if \code{newdata} is supplied.}

\item{newdata}{Optional data frame passed to \code{brms::fitted()}. If \code{NULL},
\code{fit$data} is used after applying \code{nullify} and \code{offset_value}.}

\item{re_formula}{Random-effect formula for the fitted component.
Use \code{NA} to drop all random effects; \code{~(1 | tissue_groups)} keeps
tissue intercepts.}

\item{offset}{Name of the offset column in \code{fit$data}.}

\item{offset_value}{Value written into the offset column of \code{newdata}
(default \code{0}).}

\item{robust}{If \code{TRUE}, summarise the posterior with medians.}

\item{correct_by_offset}{If \code{TRUE}, divide residuals by \code{exp(offset)}
from the original data.}

\item{sample_id}{Optional character vector of sample ids to bind onto the
result. A warning is issued if the length does not match.}
}
\value{
A tibble with \code{adjusted___}, \code{residuals___}, and \code{fitted___}
posterior summaries, one row per observation.
}
\description{
Posterior residuals (optionally divided by \code{exp(offset)}) are added to
fitted values from a newdata grid where selected covariates are set to
\code{NA} and the offset is zeroed. Random effects are included or dropped
via \code{re_formula}. This is the gene-wise form of the immuneBodyMap
\code{remove_unwanted_effect()} step.
}
\examples{
\dontrun{
data("airway", package = "airway")
se <- airway["ENSG00000120129", ]
se$offset <- log(colSums(SummarizedExperiment::assay(airway, "counts")))
se$dex <- relevel(factor(se$dex), ref = "untrt")
fit <- estimate_gene(se, ~ dex + (1 | cell), offset = "offset")

adjust_gene(
  fit,
  nullify = "dex",
  re_formula = ~(1 | cell)
)
}

}
