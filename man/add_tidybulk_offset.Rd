% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_tidybulk_offset.R
\name{add_tidybulk_offset}
\alias{add_tidybulk_offset}
\title{Add a tidybulk TMM offset to a SummarizedExperiment}
\usage{
add_tidybulk_offset(se, abundance = "counts", method = "TMM")
}
\arguments{
\item{se}{A \code{SummarizedExperiment}.}

\item{abundance}{Assay name passed to tidybulk (default first assay /
\code{"counts"}).}

\item{method}{tidybulk scaling method. Default \code{"TMM"} (HPCell). Use
\code{"TMMwsp"} to match the immuneBodyMap pipeline.}
}
\value{
\code{se} with assays \verb{*_scaled}, and \code{colData} columns \code{TMM},
\code{multiplier}, and \code{offset}.
}
\description{
Runs \code{\link[tidybulk:scale_abundance]{tidybulk::scale_abundance()}} on the full object (not gene-wise) and
stores \code{offset = log(1 / multiplier)} in \code{colData}, matching the
immuneBodyMap / HPCell convention. \code{estimate_gene()} does not compute
this offset; this helper (and \code{\link[=brmDE]{brmDE()}}) does.
}
\examples{
\dontrun{
data("airway", package = "airway")
se <- add_tidybulk_offset(airway[1:150, ])
}

}
