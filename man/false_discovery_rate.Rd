% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis_gene.R
\name{false_discovery_rate}
\alias{false_discovery_rate}
\title{False discovery rate from per-gene null probabilities}
\usage{
false_discovery_rate(pH0)
}
\arguments{
\item{pH0}{Numeric vector of per-gene null probabilities, as returned in
the \code{pH0} column of \code{\link[=hypothesis_gene]{hypothesis_gene()}}.}
}
\value{
A numeric vector the same length as \code{pH0}.
}
\description{
Turns the \code{pH0} column of \code{\link[=hypothesis_gene]{hypothesis_gene()}} into a false discovery rate
across genes. Genes are ordered by \code{pH0} and each gene is given the mean
\code{pH0} of every gene ordered at or before it, so a gene's value is the
expected proportion of false discoveries in the set obtained by cutting
there. The returned vector is in the order it was given, not in sorted
order.
}
\details{
This follows from \code{pH0} being a probability rather than a test statistic.
The expected number of false discoveries in a selected set is the sum of the
null probabilities of its members, so dividing by the size of the set gives
the expected proportion directly, with no null distribution and no p-values
in between. The cumulative mean of an ascending sequence is already
non-decreasing, so unlike Benjamini-Hochberg q-values no monotonicity step
is needed.

Two things to keep in mind. The value is a property of the gene set, so
filtering genes moves every gene's rate, exactly as it does for
Benjamini-Hochberg. And \code{pH0} is counted from posterior draws, so its
resolution is \code{1 / ndraws}: a \code{pH0} of 0 means "below what this many draws
can resolve", and a rate far below \code{1 / ndraws} is not supported by the fit
it came from.
}
\examples{
pH0 <- c(0.4, 0.001, 0.02, 0.9, 0.005)
false_discovery_rate(pH0)

}
