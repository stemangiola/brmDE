% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gene.R
\name{plot_gene}
\alias{plot_gene}
\title{Plot one gene across a factor, with a posterior predictive check}
\usage{
plot_gene(
  fit,
  factor,
  remove_unwanted_effects = FALSE,
  number_of_draws = 100,
  offset = "offset",
  feature = NULL,
  seed = NULL
)
}
\arguments{
\item{fit}{A \code{brmsfit} from \code{\link[=estimate_gene]{estimate_gene()}}.}

\item{factor}{Character string naming the factor of interest in the
abundance model, used to stratify the boxplot.}

\item{remove_unwanted_effects}{If \code{TRUE}, keep \code{factor} alone: the random
effects and every other covariate come out of both the counts and the
posterior predictive. Defaults to \code{FALSE}, where only the library size
is removed.}

\item{number_of_draws}{Number of posterior predictive draws to overlay
(blue boxplots). Defaults to \code{100}.}

\item{offset}{Name of the offset column in \code{fit$data}.}

\item{feature}{Optional gene id used as the plot subtitle. The fit does
not carry this: pass it from the pipeline result (\code{.feature}) or from
the gene you filtered on.}

\item{seed}{Optional seed passed to \code{\link[brms:posterior_predict]{brms::posterior_predict()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Boxplot of library-size-normalised counts for a single \code{\link[=estimate_gene]{estimate_gene()}}
fit, stratified by a factor in the abundance model. Blue boxplots are
posterior predictive draws: if the model is descriptively adequate they
should roughly overlay the black boxplots of the observed (or adjusted)
counts. The construction follows \code{sccomp_boxplot()}.
}
\details{
The library size always comes out of the plotted counts, through the model
rather than by dividing: \code{\link[=adjust_gene]{adjust_gene()}} predicts with the offset zeroed,
which for a TMM offset \code{log(1 / multiplier)} leaves the TMM-normalised
abundance. Setting \code{remove_unwanted_effects = TRUE} drops the random
effects and any other covariate as well, keeping \code{factor} alone, so a dex
effect is not mixed with cell-line intercepts.
}
\examples{
print("cmdstanr is needed to run this example.")
# install.packages("cmdstanr", repos = c("https://stan-dev.r-universe.dev/", getOption("repos")))

\donttest{
if (instantiate::stan_cmdstan_exists()) {
  data("airway", package = "airway")
  se <- airway["ENSG00000120129", ]
  se$offset <- log(colSums(SummarizedExperiment::assay(airway, "counts")))
  se$dex <- relevel(factor(se$dex), ref = "untrt")

  fit <- estimate_gene(
    se,
    formula_abundance = ~ dex + (1 | cell),
    offset = "offset",
    abundance = "counts"
  )

  plot_gene(fit, factor = "dex", feature = "ENSG00000120129")
  plot_gene(fit, factor = "dex", feature = "ENSG00000120129",
            remove_unwanted_effects = TRUE)
}
}

}
\seealso{
\code{\link[=estimate_gene]{estimate_gene()}}, \code{\link[=adjust_gene]{adjust_gene()}}, \code{\link[=hypothesis_gene]{hypothesis_gene()}}
}
