% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gene.R
\name{estimate_gene}
\alias{estimate_gene}
\title{Fit a brms count model for one gene}
\usage{
estimate_gene(
  data,
  formula_abundance,
  formula_dispersion = ~1,
  offset,
  dispersion = NULL,
  dispersion_degrees_freedom = NULL,
  shape_prior = c("student_t", "gamma"),
  shape_prior_df = 3,
  coefficient_prior_scale = 0.7,
  coefficient_prior_df = 3,
  family = NULL,
  abundance = "counts",
  prior = NULL,
  sample_prior = c("no", "yes", "only"),
  chains = 2,
  iter = 800,
  warmup = 300,
  backend = c("cmdstanr", "rstan"),
  cores = chains,
  init = "gene",
  sanitize_names = FALSE,
  feature = NULL,
  stanvars = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame with one row per sample, or a
\code{SummarizedExperiment} with a single feature.}

\item{formula_abundance}{Model for the mean. A \code{formula} or \code{brmsformula}
whose response may be omitted (\code{~ dex + (1 | donor)}); it is then set to
\code{abundance}. The library size \verb{offset(<offset>)} term is added here rather
than by you, and the assembled formula is reported with a message.}

\item{formula_dispersion}{One-sided model for the negative binomial shape,
\code{~1} by default. Used only when \code{shape_prior = "student_t"}, where it
becomes a \verb{shape ~ <terms> + offset(...)} submodel. The offset is
\verb{log(1/<dispersion>)} when \code{dispersion} is supplied, and \code{0} when it is
\code{NULL}, so the terms you give describe departures from that external
estimate or, with a zero offset, the log-shape itself. Do not write the
offset yourself.}

\item{offset}{Required name of the precomputed offset column in \code{data}
(or in \code{colData} if \code{data} is a \code{SummarizedExperiment}). The offset is
never calculated inside this function.}

\item{dispersion}{Optional name of a precomputed dispersion column
in \code{rowData} (or in \code{data}). Default \code{NULL} puts a zero offset on the
shape submodel: the intercept is then \code{log(shape)}, with prior median
shape \code{1}. Use this when the sample size is large enough that an
informative location from previous tools is unnecessary. When supplied,
the column becomes \verb{offset(log(1/<dispersion>))} as described under
\code{shape_prior}: that is the prior \emph{location} of the shape, which the
gene's counts are then free to update. \code{\link[tidybulk:estimate_dispersion]{tidybulk::estimate_dispersion()}}
writes this column; this function does not compute it.}

\item{dispersion_degrees_freedom}{Optional name of the effective degrees
of freedom column that accompanies \code{dispersion}, as written by
\code{\link[tidybulk:estimate_dispersion]{tidybulk::estimate_dispersion()}}. Default \code{NULL} gives the Student-t
shape intercept a log-scale SD of 1, so the prior scale does not depend
on previous tools. Independent of \code{dispersion}: you can pass one, both,
or neither.}

\item{shape_prior}{How the external dispersion is turned into a prior on
the shape parameter. The gene-wise posterior is not constrained to equal
that estimate: both forms only locate and scale the prior, and the
likelihood can move the shape away from it. Both imply the same
log-scale spread, \code{trigamma(d_eff / 2)}, but they are not
reparameterisations of one another: they differ in tail weight (how
readily a gene may diverge), and they centre different summaries of the
shape on the external estimate. Both put all prior mass on a positive
shape, one by exponentiating an unbounded parameter and the other by
bounding it.
\itemize{
\item \code{"student_t"} (default) adds a
\code{shape ~ 1 + offset(log(1/dispersion))} submodel (or \code{offset(0)} when
\code{dispersion} is \code{NULL}) and puts a Student-t prior on its intercept,
scaled by \code{shape_prior_df}. brms gives the submodel a log link, so this
is symmetric in \code{log(shape)} and centres the \emph{median} shape on
\code{1/dispersion} (or on \code{1} when the offset is 0). Heavier-tailed, so
more forgiving when edgeR has over-shrunk a gene, and the submodel
stays available for dispersion covariates.
\item \code{"gamma"} adds no submodel and instead puts
\code{gamma(d_eff/2, d_eff * dispersion/2)} on \code{shape} directly. This is the
conjugate form implied by the edgeR hierarchy, in which the dispersion
is scaled inverse chi-square and hence the precision \code{1/dispersion} is
gamma. It centres the \emph{mean} shape on \code{1/dispersion}, which sits
\code{digamma(d_eff/2) - log(d_eff/2)}, roughly \code{-1/d_eff}, from the
Student-t centre on the log scale. Lighter-tailed. Needs both
\code{dispersion} and \code{dispersion_degrees_freedom}, or neither (in which
case brms' vague \code{gamma(0.01, 0.01)} is used). A scalar \code{shape}
carries no linear predictor, so a \code{formula_dispersion} with terms is an
error here.
}}

\item{shape_prior_df}{Degrees of freedom \eqn{\nu} of the Student-t prior
on the shape intercept, used only when \code{shape_prior = "student_t"}. This
single value sets both the \code{df} argument of \code{student_t(df, 0, scale)} and
the \code{scale}, because a Student-t has standard deviation
\code{scale * sqrt(nu / (nu - 2))}; the scale is therefore
\code{sd * sqrt((nu - 2) / nu)} for a target \code{sd}. Must exceed 2, since the
Student-t standard deviation does not exist at or below 2 degrees of
freedom. The target \code{sd} is \code{sqrt(trigamma(d_eff / 2))} when
\code{dispersion_degrees_freedom} is supplied, and 1 when it is omitted.}

\item{coefficient_prior_scale}{Scale of the Student-t prior on the
population-level coefficients of the abundance model (\code{class = "b"}).
Those coefficients sit behind a log link, so each one is a natural-log
fold change. The default \code{0.7} is one log2 fold change, a doubling of
expression, which is the order of the effects a differential expression
analysis looks for; \code{1.4} is two log2 fold changes, and so on. Unlike
\code{shape_prior_df}, this is the Stan scale itself rather than a target
standard deviation, so it reaches the prior untouched.

This is not only a shrinkage choice. \code{\link[=hypothesis_gene]{hypothesis_gene()}} evaluates an
entry such as \code{"dextrt = 0"} by the Savage-Dickey density ratio, the
posterior density at 0 over the prior density at 0, so a wider prior thins
prior mass at 0 and moves the evidence towards the null however clear the
data are. Keep this on the order of the effects you are looking for.}

\item{coefficient_prior_df}{Degrees of freedom of that Student-t prior. The
default 3 keeps the tails heavy enough that a large fold change is still
cheap.}

\item{family}{A brms family. Default is
\code{\link[brms:zero_inflated_negbinomial]{brms::zero_inflated_negbinomial()}}.}

\item{abundance}{Name of the count column, or assay name if \code{data} is a
\code{SummarizedExperiment}.}

\item{prior}{Optional brms prior. If \code{NULL} and \code{family} is a negative
binomial (with or without zero inflation), student-t priors are used with
the intercept centred at \code{mean(log1p(counts / exp(offset)))} and the
coefficients scaled by \code{coefficient_prior_scale}. Supplying your own
\code{prior} replaces that set entirely, so give every parameter you intend to
test a proper prior; brms cannot draw from a flat one, and an \code{"= 0"}
entry on a parameter with a flat prior reports NA.}

\item{sample_prior}{Passed to \code{\link[brms:brm]{brms::brm()}}. \code{"yes"} draws from the prior
alongside the posterior, which is what lets \code{\link[=hypothesis_gene]{hypothesis_gene()}} report
\code{pH0} and \code{evid_ratio} for an entry such as
\code{"dextrt = 0"}. The draws cost no sampling work, being taken in generated
quantities, but they do enlarge every stored fit, which at transcriptome
scale is disk you would rather keep. The default \code{"no"} omits them, since
the default test in \code{\link[=hypothesis_gene]{hypothesis_gene()}} is directional and needs no prior
draws. Set this to \code{"yes"} when you intend to ask for a Bayes factor.}

\item{chains, iter, warmup}{MCMC settings. Defaults match the manuscript
pipeline (2 chains, 600 iterations, 400 warmup).}

\item{backend}{Passed to \code{\link[brms:brm]{brms::brm()}}. Default \code{"cmdstanr"}.}

\item{cores}{Number of CPU cores. Defaults to \verb{min(available cores, chains)}.}

\item{init}{Inits for \code{\link[brms:brm]{brms::brm()}}. \code{"gene"} (default) builds intercept
and coefficient inits from \code{make_standata()}. Use \code{"random"} or a list
for brms defaults / custom inits.}

\item{sanitize_names}{If \code{TRUE}, collapse repeated underscores in column
names (\verb{___} → \verb{_}) to work around a brms parsing issue.}

\item{feature}{Optional gene id used only in a warning string (handy for
targets logs).}

\item{stanvars}{Optional \code{\link[brms:stanvar]{brms::stanvar()}} object appended to the ones
brmDE builds for its own priors.}

\item{...}{Additional arguments passed to \code{\link[brms:brm]{brms::brm()}}.}
}
\value{
A \code{brmsfit} object.
}
\description{
Prepares one gene's samples (data frame or one-row
\code{SummarizedExperiment}) and fits a brms model. You give the mean and
dispersion models as plain one-sided formulas; the library size offset and the externally estimated dispersion offset are
appended here, and the assembled formulas are reported with a message. A
precomputed dispersion (typically from \code{\link[tidybulk:estimate_dispersion]{tidybulk::estimate_dispersion()}})
is used as a \strong{prior} on the negative
binomial shape, not as a fixed plug-in: the gene-wise likelihood can still
pull the posterior away from that estimate. Priors, inits, and MCMC
defaults follow the immuneBodyMap ZINB specification and can all be
overridden.
}
\details{
Formula terms are resolved against \code{data} first and the global environment
after. The environment a formula was written in is deliberately dropped:
R attaches the defining frame to every formula, so a fit produced inside a
function would otherwise serialise that whole frame - the
\code{SummarizedExperiment} included - into each stored \code{brmsfit}, once per gene.
A formula referring to a local variable (\code{~ poly(dose, k)} with a local \code{k})
will therefore not resolve it; promote such values to arguments or to the
global environment. The targets pipeline has always behaved this way, because
formulas make a round trip through disk as text.
}
\examples{
print("cmdstanr is needed to run this example.")
# install.packages("cmdstanr", repos = c("https://stan-dev.r-universe.dev/", getOption("repos")))

if (instantiate::stan_cmdstan_exists()) {
  data("airway", package = "airway")
  se <- airway["ENSG00000120129", ]
  se$offset <- log(colSums(SummarizedExperiment::assay(airway, "counts")))
  se$dex <- relevel(factor(se$dex), ref = "untrt")

  fit <- estimate_gene(
    se,
    formula_abundance = ~ dex + (1 | cell),
    offset = "offset",
    abundance = "counts"
  )
}

}
