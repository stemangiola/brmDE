% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hpc_pipeline.R
\name{hypothesis.tidytargets}
\alias{hypothesis.tidytargets}
\title{Hypothesis tests on a tidytargets pipeline}
\usage{
\method{hypothesis}{tidytargets}(
  x,
  hypothesis,
  target_input = "brms_fit",
  target_output = "hypothesis_tbl",
  ...
)
}
\arguments{
\item{x}{A \code{tidytargets} pipeline from \code{\link[=brmDE]{brmDE()}}.}

\item{hypothesis}{Effects to test, passed to \code{\link[=hypothesis_gene]{hypothesis_gene()}}.}

\item{target_input}{Name of the upstream fit target.}

\item{target_output}{Name of the targets output.}

\item{...}{Passed to \code{\link[=hypothesis_gene]{hypothesis_gene()}}.}
}
\value{
The updated \code{tidytargets} pipeline.
}
\description{
Appends a \code{\link[tidytargets:tt_iterate]{tidytargets::tt_iterate()}} step that calls \code{\link[=hypothesis_gene]{hypothesis_gene()}}
on each \code{\link[=estimate]{estimate()}} fit. This is an S3 method for
\code{\link[brms:hypothesis]{brms::hypothesis()}}.
}
\details{
Each gene's table holds the posterior statistics for every contrast and the
convergence of that contrast's own draws, so this is where a run of 20,000
genes becomes something you can read: it is small enough to return in full,
while the fits stay in the store. See \code{\link[=hypothesis_gene]{hypothesis_gene()}} for the columns.

Collecting the pipeline adds an \code{fdr} column across genes when the tests
took the directional route, which is the default. That is the one quantity
a gene-wise target cannot compute, since it is a property of the gene set.
See \code{\link[=false_discovery_rate]{false_discovery_rate()}}.
}
