% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis_gene.R
\name{hypothesis_gene_scope}
\alias{hypothesis_gene_scope}
\title{Hypothesis tests at one brms scope}
\usage{
hypothesis_gene_scope(
  fit,
  hypothesis,
  test_above_log2FC = 1,
  scope = "standard",
  component = "fixed",
  grouping_label = "population",
  group_arg = NULL,
  class = NULL,
  robust = TRUE,
  alpha = 0.05,
  ...
)
}
\arguments{
\item{fit}{A \code{brmsfit} from \code{\link[=estimate_gene]{estimate_gene()}}.}

\item{hypothesis}{Effects to test, as in \code{\link[=hypothesis_gene]{hypothesis_gene()}}.}

\item{test_above_log2FC}{Magnitude an effect has to exceed, as in
\code{\link[=hypothesis_gene]{hypothesis_gene()}}.}

\item{scope}{Passed to \code{\link[brms:hypothesis]{brms::hypothesis()}}: \code{"standard"} for population-level
coefficients, \code{"coef"} for group-specific ones.}

\item{component}{Value written to the returned \code{component} column.}

\item{grouping_label}{Value written to the returned \code{group} column when brms
does not supply one.}

\item{group_arg}{Grouping factor passed to \code{\link[brms:hypothesis]{brms::hypothesis()}} when
\code{scope = "coef"}. \code{NULL} otherwise.}

\item{class, robust, alpha, ...}{Passed to \code{\link[brms:hypothesis]{brms::hypothesis()}}.}
}
\value{
A tibble with the same columns as \code{\link[=hypothesis_gene]{hypothesis_gene()}}.
}
\description{
Evaluates the same hypotheses either as population-level coefficients
(\code{scope = "standard"}) or as group-specific ones (\code{scope = "coef"}).
\code{\link[=hypothesis_gene]{hypothesis_gene()}} always runs the population-level scope; when \code{group} is
set it stacks a second call at \code{scope = "coef"}.
}
